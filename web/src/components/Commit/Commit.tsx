import React from 'react';

// import { Container } from './styles';

const Commit = (props: { message: string} ) => {
  return (
      <>
        <p>Commit 1</p>
      </>
  );
}

export default Commit;